{
  "mcpServers": {
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}"
      }
    },
    "airtable": {
      "command": "sh",
      "args": ["-c", "case \"$AIRTABLE_API_KEY\" in *.*) ;; *) AIRTABLE_API_KEY=$(security find-generic-password -w -a \"$USER\" -s autocompany-airtable-pat 2>/dev/null);; esac; export AIRTABLE_API_KEY; exec npx -y airtable-mcp-server"],
      "env": {
        "AIRTABLE_API_KEY": "${AIRTABLE_API_KEY}"
      }
    },
    "linear": {
      "command": "sh",
      "args": ["-c", "case \"$LINEAR_API_KEY\" in lin_api_*) ;; *) LINEAR_API_KEY=$(security find-generic-password -w -a \"$USER\" -s autocompany-linear-key 2>/dev/null);; esac; export LINEAR_API_KEY; exec npx -y @tacticlaunch/mcp-linear"],
      "env": {
        "LINEAR_API_KEY": "${LINEAR_API_KEY}"
      }
    },
    "browseros": {
      "type": "http",
      "url": "http://172.17.0.1:9245/mcp"
    }
  }
}
